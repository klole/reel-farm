# Luna handoff — CH-001R-r8

## State

- Operational status: `READY_FOR_ROUTER_PUBLISH`.
- Target application version: `0.1.0`.
- Root state: `awaiting_review`.
- Application acceptance: `false`.
- Accepted application version: `none`.
- This is the bounded CI-helper/actionlint and environment-isolation repair only; it is not v0.2 or full acceptance.

## Identities

| Field | Actual value |
|---|---|
| Packet P8 and tree | `f2d6baa075af14fbe74e836240198a90ea5d4fd1` / `a832532aad3956687c169c57c3c6e5c8adff51a1` |
| Prior T7 and tree | `6b02857401a9b1e81e1bd36d5a4418f90903adcb` / `fdbe7da15964659d4e2514580f6606f5731790ce` |
| Prior E7 | `6e4f9b3d288b7177ebfc8b49ff3c833610640060` |
| T8 implementation and tree | `0144f6c41ae4c6143a2dc46fe22d59d453ce8763` / `f16f32465b37439774369ddf002a22063c104088` |
| T8 parent | `f2d6baa075af14fbe74e836240198a90ea5d4fd1` |
| T8 workflow blob / file SHA-256 | `fb4d3e4ee58eff81e43395fa6df99c53b65de417` / `0ca1701d63be141fccff778c03e199cd3a55c5502d186000adf0e4bd545eac6c` |
| Evidence E8 | Evidence/state-only child of T8; full SHA/tree returned after commit and not embedded self-referentially |
| Local origin/main at handoff | `f2d6baa075af14fbe74e836240198a90ea5d4fd1` (P8); T8/E8 not pushed |

Machine-readable records are in [`docs/evidence/CH-001R-r8/`](../docs/evidence/CH-001R-r8/), with the checklist in [`r8-results.json`](../docs/evidence/CH-001R-r8/r8-results.json).

## Historical failure and repair disposition

The supplied hosted run `34678495442`, attempt 1, job `103512556268`, was inspected from the packet and not rerun. It checked out T7, reached Node `20.19.2`, then reported `50` CI tests with `45` pass, `5` fail, and `0` skipped. R7-T01 through R7-T04 failed because no actionlint executable was available; R7-T07 failed because the old test helper reintroduced the deleted `RUNNER_TEMP` from the real hosted parent. The outer result was `CI_BOOTSTRAP_FAILURE`, primary stage `ci-helper-regressions`, `proof_invoked=false`, and `proof_exit_code=null`. No application gate count was inferred.

R8-F01 is repaired by [`scripts/ci/actionlint-bootstrap.mjs`](../scripts/ci/actionlint-bootstrap.mjs) and workflow ordering. The helper uses the checked-in official v1.7.7 Linux/amd64 provenance, bounds download/redirect/timeout behavior, verifies the archive digest before inspection/extraction, rejects unsafe archive members, verifies the installed executable digest and `-version`, and writes the verified absolute `ACTIONLINT_BIN` to `GITHUB_ENV` while exporting it for the consuming shell. The workflow provisions it after identity/Node and before semantic workflow validation and `node --test tests/ci`; both stages are blocking and reflected in the outer reporter.

R8-F02 is repaired in [`tests/ci/workflow-validation.test.mjs`](../tests/ci/workflow-validation.test.mjs). Complete environments are passed exactly to child processes; override maps are merged only when constructing fixtures. The hosted-like parent includes valid synthetic runner, command-file, run/attempt, and inherited sandbox-state values. Independent missing-`RUNNER_TEMP` and missing-`GITHUB_ENV` cases observe absence in the child, fail nonzero, and preserve both synthetic files. The old second merge is deliberately reproduced and demonstrates reintroduction plus contamination of the wrong file.

The architect reproduction fixture has only a non-semantic `/* global process */` ESLint declaration so the required repository-wide lint command is clean. No application, sandbox, dependency, native pin, or acceptance-contract behavior was changed.

## Validator and prefix evidence

| Check | Actual result |
|---|---|
| Clean final prefix | Run `800000016`, independent checkout at T8, no `node_modules`, Node `v20.19.2`; official actionlint download, validation, and CI exit `0`; `58/58` tests pass, `0` fail, `0` skipped |
| Hosted-like final prefix | Run `800000017`, independent checkout at T8, no `node_modules`, inherited runner/command-file/sandbox values; official actionlint download, validation, and CI exit `0`; `58/58` pass, `0` fail, `0` skipped |
| Actionlint archive | v1.7.7 official Linux/amd64; expected/measured archive SHA-256 `023070a287cd8cccd71515fedc843f1985bf96c436b7effaecce67290e7e0757` |
| Actionlint executable | expected/measured SHA-256 `9f7dedb4e23f89f2922073d1a6720405b7b520d4f5832ebb96f0d55a2958886c`; observed version `1.7.7` |
| Workflow validation | Real actionlint accepts the one current tracked workflow; complete frozen T6 and semantic-invalid fixtures reject as expected; direct validation tests `13/13` pass |
| Command files | Both prefixes append one `ACTIONLINT_BIN` entry to their synthetic `GITHUB_ENV`; `GITHUB_PATH`, `GITHUB_STEP_SUMMARY`, and `GITHUB_OUTPUT` are byte-preserved |

The exact prefix commands and temporary paths are recorded in [`clean-prefix.json`](../docs/evidence/CH-001R-r8/clean-prefix.json), [`hosted-like-prefix.json`](../docs/evidence/CH-001R-r8/hosted-like-prefix.json), and [`commands.log`](../docs/evidence/CH-001R-r8/commands.log). The final actionlint attestations include requested/actual implementation identity, workflow blob/file hashes, run/attempt, platform, URL, expected/measured digests, path, version, and exit.

## Actual scoped command results

All commands below were run against final T8 with the native pinned pnpm bootstrap path. The timestamped record is [`commands.log`](../docs/evidence/CH-001R-r8/commands.log).

| Command | Exit | Actual result |
|---|---:|---|
| `node --check` helper/reporter/test modules; `bash -n` initializer | 0 | All syntax checks passed |
| `pnpm install --frozen-lockfile --reporter=silent` | 0 | Pinned install passed; native pnpm is `12.3.4` |
| `pnpm lint:workflow` | 0 | Real actionlint `1.7.7` |
| `pnpm test:ci` | 0 | 5 file-level subtests pass; final no-node_modules prefixes report 58 nested tests pass each |
| `pnpm lint` | 0 | ESLint clean |
| `pnpm typecheck` | 0 | TypeScript build/no-emit checks pass |
| `pnpm build` | 0 | All package builds and Next production build pass |
| `pnpm test:unit` | 0 | 3 files, 20 tests pass |
| `pnpm test:security` | 0 | 1 file, 4 tests pass |
| `git diff --check` | 0 | Clean |

Focused final CI counts include actionlint bootstrap `4/4`, reporter `11/11`, environment/workflow validation `13/13`, and the complete `node --test tests/ci` prefix result `58/58`; all have zero failures and skips. [`ci-result-cli.json`](../docs/evidence/CH-001R-r8/ci-result-cli.json) records the outer reporter check: new prerequisite attestation fields survive stage recording/finalization, an uninvoked proof remains `false`/`null`, and the classification remains bootstrap failure rather than a fabricated proof result.

## R8 checklist

| ID | Status | Evidence / boundary |
|---|---|---|
| R8-T01 | PASS | Packet hosted receipt, failure mapping, T7/E7 preservation, and scope comparison |
| R8-T02 | PASS | Two real Linux/amd64 official downloads before CI tests; exact version/path/digests |
| R8-T03 | PASS | Archive/executable/version/platform/unsafe-member/download negative tests fail closed |
| R8-T04 | PASS | Same verified absolute path reaches subsequent processes; prior env entries preserved |
| R8-T05 | PASS | Real validator rejects frozen T6 and semantic-invalid fixtures and accepts current workflow |
| R8-T06 | PASS | Missing `RUNNER_TEMP` remains absent; child nonzero; fixture/parent files unchanged |
| R8-T07 | PASS | Missing `GITHUB_ENV` remains absent independently; child nonzero; fixture/parent files unchanged |
| R8-T08 | PASS | Invalid inputs, literal values, inherited state, no-tree, and prior-evidence cases pass |
| R8-T09 | PASS | Legacy second merge reintroduces both keys and contaminates the wrong synthetic file |
| R8-T10 | PASS | Clean and hosted-like prefixes: 58 discovered/executed/passed, 0 failed/skipped |
| R8-T11 | PASS | New blocking stages and nullable uninvoked proof propagate through the outer reporter |
| R8-T12 | PASS | Frozen install, complete scoped commands, hashes, and unchanged frozen surfaces pass |
| R8-T13 | NOT_RUN | Router-owned publication-tree and W8 validation |
| R8-T14 | NOT_RUN | Router-owned single fresh hosted dispatch and independent receipt |

The machine-readable checklist is [`r8-results.json`](../docs/evidence/CH-001R-r8/r8-results.json). R8 results are repair evidence only and do not replace the original 72-gate contract.

## Router handoff and stop

- Publication verification: `NOT_RUN`.
- T8/E8 publication: not performed from Luna; `origin/main` remains P8.
- Hosted dispatch: `false`; no new run, job, artifact, sandbox, worker, or proof identifiers exist.
- Historical `34678495442` was not rerun.
- R8-T13/T14 remain `NOT_RUN` until the authorized router independently confirms the publication tree, exact W8 bytes, pinned validator, and pre-install rehearsal, then optionally submits at most one fresh manual dispatch with T8.
- No provider, publishing, account authorization, paid action, external browser, privileged policy operation, release/deployment, video, scheduling, billing, or v0.2 action occurred. The only external downloads were the pinned actionlint release for the final clean/hosted-like rehearsals and the pinned native pnpm release bootstrap.

`application_acceptance=false`, accepted application version `none`, and root state `awaiting_review` remain in force. Stop here for router publication and architect review.
