# CH-001R-r13 Luna / router handoff

## Disposition

```text
status=IMPLEMENTED_READY_FOR_HOSTED_MIGRATION_VERIFICATION
router_state=READY_FOR_ROUTER_PUBLISH
application_acceptance=false
accepted_application_version=none
root_state=awaiting_review
Luna_hosted_request_count=0
```

This is the bounded v0.1.0 source repair for F13-01 and F13-02. It is not v0.2, full acceptance, an unchanged T12 hosted dispatch, or permission for providers/publishing/release work. Luna did not push or dispatch and stops for router/architect review.

## Identities

- Canonical E12: `b660df14eaed8892d6ce1b8c4d5fc70ce2bfad2d`, tree `71ce94e0897a2c49edf1b9f7e372fcc85e68751d`.
- P13 architect-pack head: `34be09f3a211aae01582270708b565b4a7cce539`, tree `634a57f4d33b83e8884abd71fe797a126dedb14b`, parent E12.
- T13 implementation: `6e04b5a5eefe2ef464572da35c88338fa342f525`, tree `405a6ddc15d54ed7f9b31ce08c53af98afcd8cf8`, parent P13.
- T12 baseline: `cc9da96079fc681ce162a99bd4e3df21f05cb382`, tree `ef10ce662f60c42f0c566c0d13cf0c698710ccd9`.
- The evidence/state commit E13 is returned externally after commit and is intentionally not embedded in this handoff or its own evidence.
- Workflow `.github/workflows/ch001-live-proof.yml`: blob `35fa339aac2fcb024cd476ff38d87d27eb6482af`, SHA-256 `733643038bc7bc162265abf895e5348b1a0ca9039675740a77e0f5798b3efd2d`.
- `pnpm-lock.yaml`: SHA-256 `77861bac2106333c55ea960422cd0b34bca86dc50db2b7806ad7581c3d975576`; exact bytes preserved.

The exact ten-file implementation delta and all frozen-surface hashes are in [`docs/evidence/CH-001R-r13/scope-comparison.json`](../docs/evidence/CH-001R-r13/scope-comparison.json). The r13 checklist is [`docs/evidence/CH-001R-r13/r13-results.json`](../docs/evidence/CH-001R-r13/r13-results.json): 9 PASS, 0 FAIL, 5 NOT_RUN.

## Findings

| Finding | Actual repair | Negative and positive cases | Limitation |
|---|---|---|---|
| F13-01 fixture database routing | Added a production Node-builtins-only migration command builder/adapter with required explicit `database` and `user` targets. The marker-regression setup path passes `oss` for role/database administration and `fixtureDatabase` for fixture table, insert, ACL, observer, and denied-observer work; cleanup returns to `oss`. | The real adapter boundary is exercised by a database-aware fake. Omitted targets are rejected; old `oss` routing fails in both empty-main and existing-main witnesses; corrected fixture routing asserts actual `-d`/`-U`; early-stop and cleanup order are checked; a frozen T12 omitted-target call is retained as a source witness. | The fake is source-bound evidence, not a PostgreSQL runtime result. The capable runtime path remains wired but could not run here. |
| F13-02 invocation record identity | New r13 serialization carries captured `invocationId` into `CommandRecord`, retains exact duplicate command text and distinct evidence, emits matching child invocations, and validates strict invocation-v2 bindings. Required-command checks inspect every matching invocation. | Unit fixtures accept legitimate repeated text and reject duplicate/missing/mixed IDs, stale bindings, reused logs, missing evidence, and legacy duplicate text. Pass-then-fail and fail-then-pass both remain blocking. The local r13 command report retains actual invocation IDs and logs. | Runtime repeated-query records were not produced because the coordinator stopped before Docker/PostgreSQL; the source serializer/validator path is tested and present. |

R12's three prior repairs remain preserved: shared fail-closed absent-table observation, private raw/public container projection with actual process identity, and one effective final-image import/terminal verdict. The migration SQL/command, Docker/Compose, workflow, dependency/lock, pins, worker sandbox, and original 72 gates remain unchanged.

## Source checks

The pinned matrix passed on Node `20.19.2`, pnpm `12.3.4`, and actionlint `1.7.7`: frozen install, real workflow lint, complete `tests/ci`, clean and hosted-like detached no-`node_modules` prefixes, lint, typecheck, build, unit `24/24`, security `4/4`, changed-module syntax, `git diff --check`, and real post-build unaliased `@oss/db` import with isolated missing-link negative control.

Both prefixes were run against final T13. Each reports 10/10 file-level CI modules and 85/85 nested cases, with no skips or failures. The rehearsal utility retains its historical `CH001R_R9_SOURCE_BOUND_PREFIX` label; the T13 identity fields, command logs, and this handoff bind the records to r13. Full commands and retrievable outputs are in [`docs/evidence/CH-001R-r13/commands.md`](../docs/evidence/CH-001R-r13/commands.md).

## Runtime results

No runtime result is claimed. `r13-local-20260915c` exited `2` as `BLOCKED_ENVIRONMENT`: Docker/Compose was unavailable, loopback allocation returned `EPERM`, and pinned Chromium was unavailable. Final-image import, PostgreSQL fixture observer regression, main marker absence, shipped migration, schema/marker/sentinel, repeat, restricted-role failure, fixture/container cleanup, worker readiness, browser, and application stages are `NOT_RUN`. No SQLSTATE, image ID, container exit, marker count, schema result, or migration success is represented as observed.

The local public command report and report-referenced logs are reproduced under [`docs/evidence/CH-001R-r13/local-proof/`](../docs/evidence/CH-001R-r13/local-proof/). It records the real host module-import PASS separately from database proof, and records `4 PASS / 0 FAIL / 68 NOT_RUN` for the original application gate observation.

## Command evidence

The local report `local-proof/artifacts/ch001r13/r13-local-20260915c/public/command-report.json` has `command_record_format=invocation-v2`. It retains multiple actual invocation IDs for repeated command text, with distinct timestamps and public logs, and matching `child_invocations` bound to run `r13-local-20260915c` and T13. Unit evidence exercises duplicate/missing/mixed ID rejection, stale/missing evidence, reused logs, legacy duplicate-text rejection, and both required-command failure orders. Literal command text is unchanged and no earlier observation is deduplicated or hidden.

## Remaining blocker and external actions

The remaining blocker is host capability, not a source-test failure: runtime-only rows R13-T09 through R13-T11 remain `NOT_RUN`. Router rows R13-T13 and R13-T14 also remain `NOT_RUN` because publication and the one deliberate fresh T13 request are router-owned. No push, workflow dispatch, provider/publishing action, credential use, agent spawn, historical T11/T12 rerun, release, or v0.2 work occurred.

## Complete checklist and router stop

The complete checklist is [`docs/evidence/CH-001R-r13/r13-results.json`](../docs/evidence/CH-001R-r13/r13-results.json), with source rows R13-T01 through R13-T08 and R13-T12 PASS, runtime rows R13-T09 through R13-T11 NOT_RUN, and router rows R13-T13 through R13-T14 NOT_RUN. The evidence index is [`docs/evidence/CH-001R-r13/README.md`](../docs/evidence/CH-001R-r13/README.md).

Router must independently verify T13/P13/E12 ancestry, frozen workflow bytes, source evidence, publication head, and request history before optionally electing at most one fresh T13 request under the r13 packet. Luna has no hosted run/job/artifact identity to report.
