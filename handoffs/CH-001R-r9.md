# CH-001R-r9 Luna handoff

Status: `READY_FOR_ROUTER_PUBLISH`

This is the bounded actionlint report-path/workflow-shell repair for Open Slideshow Studio v0.1.0. It is not v0.2 work, full application acceptance, or a hosted proof result. `application_acceptance=false`, accepted application version is `none`, and root state is `awaiting_review`.

## Source identities

| Item | Identity |
|---|---|
| E8 baseline | `d04a0a70d890890132041061be87742396c1909e`; tree `40b9bc095b139a19e1c42130bf5e43ca15c79cc6` |
| P9 packet | `1f7a920e437d561fd40e8524cdb11453294a6124`; tree `b98145e3bed876d880beb9c09306715ff7e02f3d` |
| T9 implementation | `6014a247b124a186a1abfcb6d65a7ef024cf8cfc`; tree `6e590387c666ee3672210d022e51fc9283e07737` |
| T9 workflow blob | `35fa339aac2fcb024cd476ff38d87d27eb6482af` |
| T9 workflow SHA-256 | `733643038bc7bc162265abf895e5348b1a0ca9039675740a77e0f5798b3efd2d` |
| Remote at handoff | `origin/main` remains P9; Luna did not push |

E9 is the evidence/state-only commit produced after this T9. Its actual SHA/tree are returned after commit and are not embedded self-referentially in the evidence that determines them.

## Repair delivered

The current workflow keeps its raw relative job-level `CI_BOOTSTRAP_REPORT` value. The actionlint bootstrap shell body validates that base is non-empty and single-line, then computes:

```text
path.resolve(path.dirname(base), "actionlint-bootstrap.json")
```

That absolute sibling is passed unchanged to both `actionlint-bootstrap.mjs --report` and `ci-result.mjs --detail-file`. The helper retains its absolute-path contract and defensive report writer. `provisionActionlint` now validates an explicit report path before any provisioning `try` block, so invalid relative/empty/newline/carriage-return inputs cannot create temp/tool/report state, run a download/tool, or modify `GITHUB_ENV`; explicit null still means no report.

The new boundary tests execute the actual current workflow `run: |` body with the real helper and reporter, cover quoted spaces and already-absolute bases, and execute the exact frozen T8 body as a negative control. The source-bound rehearsal extracts the same tracked workflow bodies and raw job environment, then runs clean and hosted-like independent no-node_modules checkouts with the real official actionlint archive. A non-semantic ESLint declaration was added to the packet’s architect-only guard excerpt so the required repository-wide lint remains clean, matching the prior r8 diagnostic treatment.

## Final local verification

All required commands passed against final T9. The restricted-sandbox frozen-install DNS failure is retained as `01-pnpm-install.log`; the identical pinned install passed on the approved network retry in `01-pnpm-install-approved.log`.

| Check | Result |
|---|---|
| Node / pnpm | `v20.19.2` / `12.3.4` |
| Syntax | Node checks for changed modules and `bash -n` passed |
| `pnpm lint:workflow` | PASS with official actionlint `1.7.7` |
| `pnpm test:ci` | PASS; 6/6 file-level subtests, 0 failed/skipped |
| Clean source-bound prefix | PASS; run `900000601`, no `node_modules`, nested 64/64 cases |
| Hosted-like source-bound prefix | PASS; run `900000602`, no `node_modules`, nested 64/64 cases |
| `pnpm lint` | PASS |
| `pnpm typecheck` | PASS |
| `pnpm build` | PASS |
| `pnpm test:unit` | PASS; 3 files, 20/20 tests |
| `pnpm test:security` | PASS; 1 file, 4/4 tests |
| `git diff --check` | PASS; staged evidence check is also recorded |

The file-level and nested counts are intentionally separate. The aggregate workflow command reports six Node runner file subtests. The source-bound nested probe imports and executes the six tracked modules in a real Node test context: actionlint bootstrap `6`, workflow boundary `4`, CI repair `11`, proof boundary `12`, sandbox `18`, and workflow validation `13`, totaling 64 passed cases. No hosted count is included.

## Prefix evidence

The durable records are:

- [`docs/evidence/CH-001R-r9/prefix-clean.json`](../docs/evidence/CH-001R-r9/prefix-clean.json) and its [`raw commands`](../docs/evidence/CH-001R-r9/prefix-clean.commands.log)
- [`docs/evidence/CH-001R-r9/prefix-hosted-like.json`](../docs/evidence/CH-001R-r9/prefix-hosted-like.json) and its [`raw commands`](../docs/evidence/CH-001R-r9/prefix-hosted-like.commands.log)

Both prefixes report the raw relative report paths, absolute sibling target, one shared helper/recorder target, all child exit codes, official actionlint expected/measured archive SHA-256 `023070a287cd8cccd71515fedc843f1985bf96c436b7effaecce67290e7e0757`, expected/measured executable SHA-256 `9f7dedb4e23f89f2922073d1a6720405b7b520d4f5832ebb96f0d55a2958886c`, version `1.7.7`, environment-file transfer, unchanged path/output/summary sentinels, and absent proof/policy/sandbox-state trees. The hosted-like parent carries only synthetic inherited sandbox state and keeps `CH001_SANDBOX_OPT_IN=false`.

## Historical T8 hosted failure

The settled run `34928718810` attempt 1, job `104252240220`, remains read-only. Its T8 implementation was `0144f6c41ae4c6143a2dc46fe22d59d453ce8763`, workflow definition `d04a0a70d890890132041061be87742396c1909e`, and workflow blob `fb4d3e4ee58eff81e43395fa6df99c53b65de417`. The actual API-listed/downloaded artifact is `10380711446`, named `ch001-live-proof-34928718810-1`, 5558 bytes, SHA-256 `3c3905e781479fb627481c6bbf5a0bbea03ae1ab93d323cfda5b1475415db92f`. The router-message ID `10380718846` is preserved only as a discrepancy. The run was `CI_BOOTSTRAP_FAILURE` at `actionlint-bootstrap`, with `proof_invoked=false`, `proof_exit_code=null`, and application gate counts null. The original ZIP and inspection are unchanged at [`historical-hosted-receipt.json`](../docs/evidence/CH-001R-r9/historical-hosted-receipt.json) and the reviewed packet’s evidence path.

## Scope and router handoff

[`r9-results.json`](../docs/evidence/CH-001R-r9/r9-results.json) records R9-T01 through R9-T12 PASS, with R9-T13/T14 NOT_RUN and router-owned. [`scope-comparison.json`](../docs/evidence/CH-001R-r9/scope-comparison.json) records frozen application, worker/renderer, sandbox, Docker/Compose, dependency/native/Node/pnpm/Playwright, verifier, and original 72-gate surfaces unchanged. No providers, credentials, publishing, deployment, release/tagging, hosted dispatch, sandbox qualification, application proof, or v0.2 work occurred.

Router next actions remain separate: independently verify publication ancestry and W9 bytes, publish T9/E9 through the authorized path, and only then decide whether to use the packet’s one conditional manual T9 request. Luna did not dispatch or rerun the historical failure.
