# Luna handoff — CH-001R-r3

## Result

`NEEDS_WORKFLOW_DISPATCH`

Application acceptance: **false**. Target remains v0.1.0; root state remains `awaiting_review`; accepted application version remains `none`.

This is not a live-proof pass. The implementation and coordinator are committed locally at T3, but the prepared workflow could not be published with the current GitHub OAuth connection, and the local editing host lacks the runtime prerequisites. No CI run or artifact is claimed.

## Identity

- Repository/branch: `klole/reel-farm` / `main`.
- Starting commit: `a5afe2d8d1bc7741a0276513f3f1111d3ff587ae`.
- Reviewed prior T: `b516dab843be1b8870d3516185b52905982aec1f`.
- Reviewed prior E: `5a931feb01ed8da16eb9f0079a380c61f5459792`.
- Packet commit: `04b85402c03404fc9f707983594cb796525f6563`.
- New implementation T3: `0b79ed07a25a618ab4da2bf56a2fed6047398cb5`.
- T3 tree digest: `2852c9a96595e9aca234aa30f449ec615a4b43bd`; tracked tree was clean when proof ran. Pre-existing untracked dispatch/log files were preserved and not used as evidence.
- Workflow: `.github/workflows/ch001-live-proof.yml`; Git blob SHA `4a1b045e4771901713a2ad00705a0a84c9ff1dab`; file SHA-256 `c94526b66f6f21f2d4228491854f8df1e2b666796816f3d3b18e7da6c8313f1d`.
- Evidence E3: this docs/evidence commit; resolve its full SHA after commit creation and return it externally. No T3 source files were changed after the clean proof run.
- Local run: `r3-local-t3-preflight`, UTC `2026-09-12T01:18:38Z`–`2026-09-12T01:18:59Z`.
- Host: managed Linux x64, Node `v20.19.2`, pnpm `12.3.4`, Playwright `1.63.0`. CI run/job/artifact: none.

## Assigned proof outcomes

| Step | Result | Evidence/case reference | Actual gap or observation |
|---|---|---|---|
| Final image builds and sandboxed worker starts | NOT_RUN | Local proof preflight / `environment.json` | Docker/Compose absent; no shipped image or worker was started. |
| Empty migration + repeat invocation | NOT_RUN | `proof-result.json` | No run-owned database was available. |
| Unit/security/test-source checks | PASS | Coordinator command report | Lint, strict typecheck, build, 20 unit tests, and 4 security tests exited 0. |
| Real integration suite executes | NOT_RUN | `suite-integration.json` | Exit 2; 0 discovered / 0 executed because the designated prerequisites were unavailable. |
| Owner setup/login and seven-slide UI journey | NOT_RUN | `suite-e2e.json` | No pinned browser or loopback service; 0 discovered / 0 executed. |
| Real renderer suite executes | NOT_RUN | `suite-render.json` | No qualified Chromium; 0 discovered / 0 executed. |
| Canonical ZIP and preview-byte equality | NOT_RUN | `proof-result.json` artifact checks | No ZIP, preview bytes, or hashes were produced. |
| Alternate 4:5 output dimensions | NOT_RUN | `proof-result.json` artifact checks | No alternate render/export was produced. |
| Worker stop/queued request/restart/download | NOT_RUN | `proof-result.json` | No worker lifecycle was possible without Compose. |
| Same data/artifact after container recreation | NOT_RUN | `proof-result.json` | No containers or persistent volumes were created. |
| Evidence sanitization/retrievability | PASS — local only | `sanitization.json`, `artifact-manifest.json` | Sanitized non-secret preflight reports were generated under the ignored local public path; no hosted artifact is retrievable yet. |

The coordinator's fresh parent ledger covers exactly 72 IDs: `4 PASS / 0 FAIL / 68 NOT_RUN`. PASS is limited to `CH001-067`, `CH001-068`, `CH001-069`, and `CH001-072` source-only E1 inspection. `CH001-001`–`CH001-066`, `CH001-070`, and `CH001-071` remain NOT_RUN. No partial runtime observation is promoted to a full gate PASS.

## Commands and actual results

The clean run was `CH001_RUN_ID=r3-local-t3-preflight pnpm proof:ch001`. It returned exit `2` with status `BLOCKED_ENVIRONMENT`. Its public reports, child invocation IDs, UTC timestamps, and sanitized logs are in the shared workspace at `artifacts/ch001r3/r3-local-t3-preflight/public/`; the durable index is [docs/evidence/CH-001R-r3/README.md](../docs/evidence/CH-001R-r3/README.md).

The coordinator recorded these actual command outcomes:

| Command/probe | Result |
|---|---|
| `docker version`, `docker info`, `docker compose version` | exit 1 / `spawn docker ENOENT` |
| Loopback port probe | failed / `listen EPERM: operation not permitted 127.0.0.1` |
| Managed Chromium executable and sandbox launch probe | failed; expected path was not executable and launch could not complete |
| `pnpm lint` | exit 0 |
| `pnpm typecheck` | exit 0 |
| `pnpm build` | exit 0 |
| `pnpm test:unit` | exit 0; 20 tests passed |
| `pnpm test:security` | exit 0; 4 tests passed |
| integration, E2E, render, Compose proof suites | unavailable reports; each discovered/executed 0 cases |
| `pnpm verify:ch001` | not run as a full acceptance attempt; it remains strict and must stay nonzero while mandatory gates are incomplete |

The nested smoke report's Docker probe has exit `1`; the coordinator's externally classified result is exit `2` because the prerequisite failure was measured before assigned live assertions. This is not an assertion pass and not a full-verifier pass.

## Artifacts

The local public packet contains `proof-result.json`, `environment.json`, `command-report.json`, `gate-results.json`, `verifier-result.json`, suite-unavailable reports/logs, `finding-dispositions.json`, `sanitization.json`, `artifact-manifest.json`, and source/unit/security reports. It does not contain a canonical ZIP, alternate ZIP, screenshots, preview images, lifecycle comparison, or image/ZIP hash set because those files were not produced. No placeholder artifact was created.

## Findings

| Finding | Disposition | Specific source change | Executed regression or remaining requirement |
|---|---|---|---|
| U01 migration bootstrap | Fixed in source; live open | `scripts/migrate.ts` now creates/locks metadata and applies migration transactionally, including repeat invocation. | Typecheck/build passed; empty-database first/second migration remains NOT_RUN until Compose executes. |
| U02 integration import/discovery | Fixed in source; live open | Test lifecycle imports/types and dedicated test configuration were repaired. | Strict typecheck and unit/security checks passed; real integration discovery remains NOT_RUN. |
| U03 E2E UI/identity/fixture | Fixed in source; live open | Canonical roles/layouts, controlled upload/save regression, full render IDs, and fixture paths were added. | E2E did not execute; browser journey and identity assertions remain NOT_RUN. |
| U04 evidence paths/merge | Fixed in source; live open | Evidence paths are normalized/validated and fresh reports preserve PASS/FAIL/NOT_RUN without stale merging. | Verifier/unit tests passed; real evidence package/artifact assertions remain NOT_RUN. |
| U05 runner ownership/pinned browser | Fixed in source/workflow; dispatch open | The coordinator owns fresh run paths and qualifies the managed browser; workflow pins official actions and installs the project browser. | Local preflight measured unavailable browser; hosted workflow has not run. |
| U06 verifier/failure/identity semantics | Fixed in source; live open | Actual HEAD/tree identity, strict verifier mode, report identity and truthful exit semantics were added. | Static tests passed; full 72-gate acceptance remains incomplete. |
| U07 actual lifecycle assertions | Implemented; live open | Compose/lifecycle helpers cover worker stop/queue/restart and same-data recreation. | Docker unavailable; no lifecycle assertion executed. |
| U08 remaining parent coverage | Still open by scope | Bounded proof emits a fresh partial 72-ID ledger and preserves the strict full verifier. | 4 source-only PASS / 68 NOT_RUN; manual, visual, security/concurrency/recovery and other parent gates remain. |

Original R01–R11 remain open for acceptance. T3 supplies scoped harness/identity/evidence repairs and static regressions, but no unavailable runtime case is closed:

- R01/R03/R04/R07/R08: migration, shipped image, sandbox, readiness and lifecycle paths are implemented; Docker/container execution is outstanding.
- R02/R06/R09/R10: strict report/evidence semantics, save/upload/recovery, bounded multipart handling and render readiness are implemented; real adversarial/browser execution is outstanding.
- R05: upload refresh/history preservation is implemented; authenticated UI execution is outstanding.
- R11: sanitized local preflight evidence is generated; no CI-retained proof artifact or complete manual/visual review exists.

## Exact external action

`NEEDS_WORKFLOW_DISPATCH` is returned because the prepared workflow cannot be published or dispatched through the current GitHub connection. The attempted `git push origin main` reached GitHub but was rejected:

```text
refusing to allow an OAuth App to create or update workflow ".github/workflows/ch001-live-proof.yml" without "workflow" scope
```

Using the repository's already-authorized workflow-capable write path, publish the exact T3 commit `0b79ed07a25a618ab4da2bf56a2fed6047398cb5` to `main`. Once the workflow is present on the default branch, run exactly:

```bash
gh workflow run ch001-live-proof.yml --repo klole/reel-farm --ref main -f implementation_sha=0b79ed07a25a618ab4da2bf56a2fed6047398cb5
gh run list --repo klole/reel-farm --workflow ch001-live-proof.yml --event workflow_dispatch --limit 5
```

Verify that the selected run's `implementation_sha` input is T3, then retain the uploaded sanitized artifact and return its actual run/job/artifact identities. Do not substitute the old T, local blocked output, or an unrelated latest run.

## Boundaries and stop

- Provider calls/spend: none. No provider credentials or real account credentials were used.
- Repository writes: T3 implementation commit was created locally; this docs/evidence commit is E3. The push was attempted but rejected by the workflow-scope restriction. Pre-existing untracked dispatch notes/logs were preserved.
- Cleanup: no Docker project, database, named volume, or ordinary application data was created or deleted. The coordinator's run-owned directory is local and ignored.
- Exclusions honored: no fal.ai, ScrapeCreators, Pinterest, TikTok, publishing, scheduling, analytics, billing, video, public deployment, release, release/tagging, or v0.2 work.

Root state stays `awaiting_review`, accepted version remains `none`, and the 72-gate acceptance contract and historical IDs remain unchanged. Stop here for architect review.
