# CH-001R-r11 Luna handoff

Status: `READY_FOR_ROUTER_PUBLISH`

Disposition: `IMPLEMENTED_READY_FOR_HOSTED_MIGRATION_VERIFICATION`

This is the narrow Open Slideshow Studio `v0.1.0` root workspace dependency repair and migration-first qualification implementation. It is not v0.2, full acceptance, a hosted result, or a provider/publishing task.

```text
application_acceptance=false
accepted_application_version=none
root_state=awaiting_review
Luna_hosted_request_count=0
```

## Identities

| Item | Actual identity |
|---|---|
| P11 packet / starting source | `10db55ff1f942f3b1894699fea3180b69f4afe27`; tree `f1c342eef70e7f1d22818b1cbc364a687d34c985` |
| Prior T10 / E10 | `ec7cc08d6ed229af9780318858d8051202851746` / `f512c9c02620ea600404cd304782a37e6689a109`; T10 tree `9e43ac5c00801f7d523405dbe5874d64264fa7cc` |
| Final T11 implementation | `7d2a128432dd6922bee50fde94c9bd2b1bf5e49f`; tree `9b948f6247c1f7804e34bd22cf3c9ae623bde593` |
| E11 evidence/state commit | Returned after the evidence-only commit; not embedded self-referentially |
| Workflow blob / SHA-256 | `35fa339aac2fcb024cd476ff38d87d27eb6482af` / `733643038bc7bc162265abf895e5348b1a0ca9039675740a77e0f5798b3efd2d` |
| D1 receipt | `ad2aab759391c070808024ff98977f72c8cb8ec5` |

The final T11 source implementation was committed incrementally (`57e1d62`, `122e4e3`, `fde6897`, `3c966f2`, then `7d2a128`); the last full SHA/tree above is the candidate identity. The evidence-only commit follows it.

The historical negative baseline remains read-only: hosted run `34946892709`, artifact `10387768055`, digest `sha256:4c6dae545e0f778ff90bebf9216bd90258ceb41fde228a9fdff50f460a34e685`, with `ERR_MODULE_NOT_FOUND` for `@oss/db` from `/app/scripts/migrate.ts`. It was not rerun or retried.

## Authorized repair and frozen surfaces

The root `package.json` now has exactly `@oss/db: workspace:*`. `pnpm-lock.yaml` adds only:

```text
      '@oss/db':
        specifier: workspace:*
        version: link:packages/db
```

The R7-T11 pin guard remains in force and R11-T04 tests the exact allowance plus wrong/missing/extra root relationships, registry links, external dependency drift, runtime-pin drift, and lock metadata/integrity changes. The candidate lock hash is `77861bac2106333c55ea960422cd0b34bca86dc50db2b7806ad7581c3d975576` before and after `pnpm install --frozen-lockfile`; baseline was `9c49bf356bdd523623b79cf6096df83de51f06b358bffa88391700a4c5719d70`.

Unchanged from P11 are `scripts/migrate.ts`, `migrations/0001_ch001.sql`, `Dockerfile`, `compose.yaml`, sandbox qualification files, external dependency versions, Node `20.19.2`, pnpm `12.3.4`, actionlint pin `1.7.7`, workflow bytes, and the original `CH001-001` through `CH001-072` contract. `scripts/ch001-harness.ts` only admits the r11 evidence chapter; it does not change the gate IDs or acceptance semantics.

## Module import and migration-first proof

The actual post-build verifier resolves the built package to `/workspace/projects/reel-farm/packages/db/dist/index.js`, checks `pool.connect` and `pool.end`, closes the pool, and records `database_connection_attempted=false`. Its isolated temporary-root control copies the real `scripts/migrate.ts` without a root `@oss/db` link and records nonzero `ERR_MODULE_NOT_FOUND`; no fake module, alias, host SQL, or manual symlink is used. The host result is `MODULE_IMPORT_PASS_NOT_DATABASE_PROOF`, not SQL proof.

The final-image proof path is ordered as follows:

```text
build images
  -> final-image module import
  -> database start/readiness
  -> fresh-marker precondition
  -> fresh shipped migration
  -> exact schema and completion marker
  -> separate sentinel
  -> same-database repeat and invariant checks
  -> restricted-role controlled failure/no false marker
  -> fixture/container diagnostics and scoped cleanup
  -> persist migration receipt
  -> only then attempt worker/web readiness
```

The migration receipt is persisted before worker readiness and is retained if later worker/browser/application stages fail. Existing r10 stopped-service diagnostics, bounded/redacted logs, primary-failure precedence, and run-owned cleanup remain wired. Control-flow tests use fakes only to test those boundaries; they do not claim a live database result.

## Actual commands and results

All commands below were run against final T11 on Node `20.19.2`; committed summaries and sanitized records are in [`docs/evidence/CH-001R-r11/`](../docs/evidence/CH-001R-r11/).

| Command / check | Result |
|---|---|
| `pnpm install --frozen-lockfile` | PASS, exit 0; approved registry retry after offline cache lacked one tarball; lock unchanged |
| `pnpm lint` | PASS, exit 0 |
| `pnpm typecheck` | PASS, exit 0 |
| `pnpm build` | PASS, exit 0 |
| `pnpm test:unit` / `pnpm test:security` | PASS, exit 0; `20/20` / `4/4` |
| `node --import tsx scripts/verify-db-module-import.ts --negative-control` | PASS, exit 0; real import and missing-link control |
| `node --test tests/ci/migration-first.test.mjs` | PASS, exit 0; `4/4` file tests |
| clean source-bound prefix | PASS, exit 0; `8/8` files and `74/74` nested cases, no node_modules |
| hosted-like source-bound prefix | PASS, exit 0; `8/8` files and `74/74` nested cases, no node_modules; inherited sandbox state did not opt in |
| final `pnpm proof:ch001` (`r11-local-003`) | exit 2, `BLOCKED_ENVIRONMENT`; host build/module import PASS, runtime cases NOT_RUN, original ledger `4 PASS / 0 FAIL / 68 NOT_RUN` |

The direct aggregate test invocation without an actionlint executable is not used as the final CI qualification because actionlint is unavailable on this host; both source-bound rehearsals provisioned the pinned official cached archive and executed the actual current workflow bodies. The exact commands, archive/executable digests, and counts are in [`commands.log`](../docs/evidence/CH-001R-r11/commands.log), [`prefix-clean.json`](../docs/evidence/CH-001R-r11/prefix-clean.json), and [`prefix-hosted-like.json`](../docs/evidence/CH-001R-r11/prefix-hosted-like.json).

## Runtime boundary and remaining blocker

The local coordinator measured loopback `listen EPERM`, no Docker CLI/daemon/Compose, and no usable pinned Playwright Chromium executable. Therefore the final image was not built locally and no PostgreSQL database, fresh migration, schema observation, repeat, restricted-role failure, container exit, browser, worker, slideshow artifact, or runtime cleanup is claimed. `migration-verification.json` records every runtime stage as `NOT_RUN`, `worker_readiness_attempted=false`, and no allocated fixture. This is the explicitly authorized fallback, not a relabeled runtime failure.

R11 checklist: [`r11-results.json`](../docs/evidence/CH-001R-r11/r11-results.json) records `10 PASS / 0 FAIL / 8 NOT_RUN`: T01–T08, T15, and T16 passed; T09–T14 are environment-gated runtime NOT_RUN; T17–T18 are router-owned NOT_RUN. The original 72-gate proof ledger remains separate and unaccepted.

## External actions and stop condition

Luna did not push, publish, dispatch, auto-retry, rerun `34946892709`, create Docker resources, mutate host policy, use providers or credentials, release, or begin v0.2 work. The router owns source publication identity checks and at most one conditional fresh T11 hosted migration request after review. Stop here for router/architect review; do not accept v0.1 from this handoff.
