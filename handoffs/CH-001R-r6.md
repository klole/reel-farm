# Luna handoff — CH-001R-r6

## State

Operational status: `READY_FOR_ROUTER_PUBLISH / NEEDS_WORKFLOW_DISPATCH`.

Target: Open Slideshow Studio `0.1.0`. Application acceptance: **false**. Root: `awaiting_review`. Accepted application version: `none`. This is the hosted Chromium sandbox qualification continuation only; it is not v0.2 and not full acceptance.

## Source identities

- Authorized packet P6: `8fa06ddae978eb6ca8f946e77459810a7bc68cb3`, tree `c444fe48b83f6aee962c7fccb8a16562a2a3cf56`.
- T6 implementation commit: `e0ea57665d00a643a8c392dfb9f6a84a723729af`.
- T6 implementation tree: `7a5ada37e8ee88a1617ff19039fa116ced44e9c5`.
- T6 parent: P6 `8fa06ddae978eb6ca8f946e77459810a7bc68cb3`.
- T6 workflow blob: `f1ddbbfc0cbaa4602663428d569b61dbf12bd068`.
- T6 workflow file SHA-256: `e1e4f6e5ebfa61f2aae3c04bcf985d12407a8f921dfeaa34dae01d103d01af85`.
- E6 evidence-only commit: this handoff/evidence/state commit; return its full SHA externally after commit. No execution-affecting edit is permitted after T6.
- Pins compared unchanged: Node `20.19.2`, pnpm `12.3.4`, Playwright `1.63.0`, lock SHA-256 `9c49bf356bdd523623b79cf6096df83de51f06b358bffa88391700a4c5719d70`, native manifest SHA-256 `fb6212a43173722f565a4101c311b23b54b7204f4828e0c62e9b9f7af1a8bd6d`, package SHA-256 `cb1e1a5c2ec11ecc7d397c9aa7700d76841cede38615656e3e273f34b140dbd0`.

## T6 implementation

The helper at `scripts/ch001-sandbox.mjs` resolves and hashes the actual Playwright-managed executable after canonicalization, rejects outside/symlink/layout/pin drift, captures OS/kernel/uid/gid/sysctl/AppArmor/parser/status/denial/process facts, and launches the same exact executable with `headless=true`, `chromiumSandbox=true`, a synthetic render, and `chrome://sandbox` evidence. Positive qualification requires both a completed render and positive effective sandbox evidence.

The workflow accepts a required boolean `sandbox_qualification` input, but only the `true` manually dispatched public `klole/reel-farm` `workflow_dispatch` on the declared standard Ubuntu 24.04 hosted path can reach the exact temporary AppArmor `flags=(unconfined) { userns, }` exception. The request binds full requested/checked-out implementation and workflow identities, exact managed executable path/root/hash, root-owned unique profile content, and non-root uid. The root-only helper exclusively creates/parses/loads/removes that profile; it does not run the coordinator or browser as root. The coordinator uses `CH001_SANDBOX_READY=1` to permit hosted application browser gates and performs cleanup after its own browser/Compose shutdown.

Host launch paths retain `chromiumSandbox: true`: coordinator preflight, Playwright host E2E/render, and lifecycle. The container worker remains a separately qualified route with its image-local managed browser, non-root user, no-new-privileges, dropped capabilities, and unchanged Compose/Docker defaults. The worker change only removes the host executable override; no business/application feature changed.

## Local results

Required local checks against T6 all exited 0:

| Command | UTC | Result |
|---|---|---|
| `node --check scripts/ch001-sandbox.mjs` | 05:15:28 | PASS |
| `node --check scripts/ci/ci-result.mjs` | 05:15:28 | PASS |
| `pnpm test:ci` | 05:15:28–05:15:29 | PASS; 10 CI-repair, 12 R5 boundary, 18 r6 sandbox cases |
| `pnpm lint` | 05:15:29–05:15:32 | PASS |
| `pnpm typecheck` | 05:15:32–05:15:34 | PASS |
| `pnpm build` | 05:15:34–05:15:48 | PASS |
| `pnpm test:unit` | 05:15:48–05:15:50 | PASS; 20/20 |
| `pnpm test:security` | 05:15:50–05:15:51 | PASS; 4/4 |
| `git diff --check` | 05:15:51 | PASS |

See [r6-results.json](../docs/evidence/CH-001R-r6/r6-results.json), [commands.log](../docs/evidence/CH-001R-r6/commands.log), and [source-review.md](../docs/evidence/CH-001R-r6/source-review.md). The tests use temporary filesystem/fake-launch fixtures where required; they are not hosted AppArmor, worker, or application proof.

## Host qualification

- Approved hosted context: **not executed** by Luna. The helper's guard is implemented and locally tested with allowed/refused environment fixtures; no `sudo`, AppArmor load, or policy mutation occurred on this editing host.
- Local direct host snapshot: Debian 13, uid/gid `1000/1000`, no managed Chromium at `/home/box/.cache/ms-playwright/chromium-1243/chrome-linux64/chrome`, AppArmor/parser/status unavailable, `kernel.max_user_namespaces=2147483647`, requested AppArmor/userns sysctls unavailable (`null` with reasons), and no denial tied to the absent browser. See [sandbox-local-host-snapshot.json](../docs/evidence/CH-001R-r6/sandbox-local-host-snapshot.json). These facts do not qualify the hosted runner.
- Default/qualified hosted probe, exact profile hash/load, effective namespace/seccomp/`chrome://sandbox` observation, global before/after, and owned cleanup: `null until router dispatch`.
- Local bounded proof `r6-local-001`: exit `2`, `BLOCKED_ENVIRONMENT`, `4 PASS / 0 FAIL / 68 NOT_RUN`; loopback listen `EPERM`, Docker/Compose `ENOENT`, and absent pinned Chromium. No application, worker, ZIP, screenshot, or lifecycle claim was made. See [local-proof-summary.json](../docs/evidence/CH-001R-r6/local-proof-summary.json).

## Hosted outcome reserved for router

There is no current hosted run, job, artifact, or router receipt. T6 is not published from this editing box and no Actions dispatch was initiated. The router must:

1. Publish T6 and this E6 evidence-only commit through the workflow-capable repository path and verify the published full SHAs/tree.
2. Confirm the workflow blob/file identity, then dispatch at most one fresh manual run on `main` with `implementation_sha=e0ea57665d00a643a8c392dfb9f6a84a723729af` and `sandbox_qualification=true`.
3. Independently retrieve the resulting artifact, verify run/attempt/job identities, archive bytes/digest/length and referenced payloads, preserve the post-proof cleanup record, and append the router receipt without rewriting the archive.
4. Report R6-T15/T16/T17/T18/T19 from actual output. If the worker or application fails, return to architect review; do not broaden this repair or mark acceptance.

The old hosted run `34671716094` remains historical T5 evidence: bootstrap passed, proof exited `2` `BLOCKED_ENVIRONMENT` because Chromium sandbox launch failed, with `4 PASS / 0 FAIL / 68 NOT_RUN`. It is not a current r6 result and must not be reused.

## Scope and stop

No providers, account authorization, publishing, scheduling, analytics, billing, video, deployment, release/tagging, public posts, dependency upgrades, global sysctl/AppArmor changes, no-sandbox mode, root browser, unconfined container, or external browser activity occurred. Historical T5 and original CH-001 evidence remain preserved. Keep `application_acceptance=false`, accepted version `none`, and root `awaiting_review` after any bounded hosted result; stop for architect review rather than initiating the next chapter.
