# CH-001R-r10 Luna handoff

Status: `READY_FOR_ROUTER_PUBLISH`

Completion mode: `DIAGNOSTICS_READY_FOR_ROUTER_REPRODUCTION`. This is a bounded Open Slideshow Studio v0.1.0 migration-failure diagnosis and partial-startup diagnostics/cleanup repair. It is not a migration-success result, full application acceptance, or v0.2 work. `application_acceptance=false`, accepted application version is `none`, and root state is `awaiting_review`.

## Identities

| Item | Identity |
|---|---|
| E9 baseline | `0f222800f1e91a18cc2f16824a87c9513cd29026`; tree `88cb7927e45605177318448115a28c177c6071a0` |
| T9 implementation | `6014a247b124a186a1abfcb6d65a7ef024cf8cfc`; tree `6e590387c666ee3672210d022e51fc9283e07737` |
| P10 packet | `df8ce7d938910949fef3840ac2c79ec9c388dc6a`; tree `fe809452e322a858a2ec2dcdc634f712d51c714b` |
| T10 implementation | `ec7cc08d6ed229af9780318858d8051202851746`; tree `9e43ac5c00801f7d523405dbe5874d64264fa7cc` |
| Workflow blob | `35fa339aac2fcb024cd476ff38d87d27eb6482af` |
| Workflow SHA-256 | `733643038bc7bc162265abf895e5348b1a0ca9039675740a77e0f5798b3efd2d` |
| E10 evidence commit | Returned after the evidence-only commit; not embedded self-referentially |
| Publication/dispatch | Luna did not push, publish, dispatch, or rerun a hosted workflow; hosted request count `0` |

The historical hosted failure `34937329430` and artifact `10383922146` remain unchanged and read-only. The artifact digest is `sha256:4b64eb28f946bd74fd67fe14b10200b85886ed28eb42ea2847e71443ac1a96f0`; its absent migration stderr is not reconstructed.

## Observed migration cause

The exact T9 final-image migration reproduction was **NOT_RUN**. The editing host has no Docker CLI or Compose daemon, so no final-image image ID, entrypoint output, PostgreSQL SQLSTATE, schema observation, fresh migration exit, same-database repeat exit, controlled failure, or real cleanup receipt is claimed.

The host-only probe used a synthetic loopback URL and no credentials:

```text
DATABASE_URL=postgres://oss:oss@127.0.0.1:1/oss DOTENV_CONFIG_PATH=/dev/null DB_POOL_MAX=1 timeout 15s pnpm db:migrate
exit=1
Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@oss/db' imported from /workspace/projects/reel-farm/scripts/migrate.ts
```

This supports the root-script bare-package-resolution hypothesis on the host and proves only that the host invocation failed during module loading before database connection. It does not confirm the exact final-image cause. Therefore no SQL or migration edit was made and **no migration repair is claimed**. The disposition and sanitized log are in [`migration-diagnosis.json`](../docs/evidence/CH-001R-r10/migration-diagnosis.json) and [`migration-baseline.log`](../docs/evidence/CH-001R-r10/migration-baseline.log).

## Partial startup behavior repaired

T10 changes only the existing Compose/proof boundary:

- `scripts/ch001-compose-diagnostics.mjs` validates generated project/root/env ownership before allocation, parses Compose JSON-array and JSON-lines output, captures selected stopped/running state including `migrate` exit code, captures image IDs, and bounds diagnostics to 20 seconds and 128 KiB.
- Service logs use `--no-color --timestamps --tail 500`; public records redact synthetic secrets, PostgreSQL URLs, bearer tokens, and cookies while raw logs remain private to the run owner. Malformed or failed reads are recorded as unknown/failed rather than silently becoming empty state.
- `scripts/ch001-proof.ts` separates startup attempted, project owned, startup return, and readiness. It collects state/log diagnostics before `down -v --remove-orphans`, preserves the original startup error as primary, appends diagnostic/cleanup errors as secondary, and verifies only the generated project and its labeled volumes after teardown.
- Diagnostics and cleanup are finalized before public sanitization and `artifact-manifest.json` hashing. If startup was never attempted, the proof writes honest `NOT_RUN` startup/cleanup records and issues no teardown.
- [`tests/ci/compose-diagnostics.test.mjs`](../tests/ci/compose-diagnostics.test.mjs) exercises the actual helper: stopped `migrate` diagnostics, bounded log arguments, redaction/private raw split, malformed state, preexisting-project refusal, scoped cleanup, and early no-startup behavior.

No migration SQL, package pin, Dockerfile, Compose dependency, worker security, workflow, renderer, provider, publishing, or v0.2 surface changed. The frozen r5 ownership prerequisite and r6–r9 prerequisites remain intact.

## Actual verification

All results below are against T10 `ec7cc08d6ed229af9780318858d8051202851746`.

| Class | Command/result | Evidence |
|---|---|---|
| Host install | Restricted `pnpm install --frozen-lockfile` failed on registry DNS; approved network retry passed with pnpm `12.3.4` and unchanged lockfile | [`command-report.json`](../docs/evidence/CH-001R-r10/command-report.json) |
| Static/project checks | Syntax, `git diff --check`, `pnpm lint`, `pnpm typecheck`, and `pnpm build` passed | [`command-report.json`](../docs/evidence/CH-001R-r10/command-report.json) |
| Unit/security | `pnpm test:unit` passed 3 files / 20 tests; `pnpm test:security` passed 1 file / 4 tests | [`command-report.json`](../docs/evidence/CH-001R-r10/command-report.json) |
| Pinned workflow/CI | Official actionlint `1.7.7` passed; `pnpm test:ci` passed 7/7 file-level modules | [`command-report.json`](../docs/evidence/CH-001R-r10/command-report.json) |
| Source-bound clean prefix | Passed 7/7 file-level and 69/69 nested cases; no `node_modules` | Local `artifacts/ch001r10/prefix-clean.json` (gitignored) |
| Source-bound hosted-like prefix | Passed 7/7 file-level and 69/69 nested cases; no `node_modules`; sandbox opt-in false | Local `artifacts/ch001r10/prefix-hosted-like.json` (gitignored) |
| Local bounded proof | `pnpm proof:ch001` exit `2`, `BLOCKED_ENVIRONMENT`; original ledger `4 PASS / 0 FAIL / 68 NOT_RUN` | Local `artifacts/ch001r10/r10-local-proof/proof/public/` (gitignored) |
| Final-image migration fresh/repeat/schema/error | NOT_RUN; Docker/Compose unavailable | [`migration-fresh.log`](../docs/evidence/CH-001R-r10/migration-fresh.log), [`migration-repeat.log`](../docs/evidence/CH-001R-r10/migration-repeat.log), [`migration-schema-check.json`](../docs/evidence/CH-001R-r10/migration-schema-check.json) |
| Real partial-startup and sibling preservation | NOT_RUN; no run-owned Docker project was allocated | [`compose-service-logs.log`](../docs/evidence/CH-001R-r10/compose-service-logs.log) and [router reproduction](../docs/evidence/CH-001R-r10/router-reproduction.md) |
| Hosted T10 | NOT_RUN; Luna dispatched zero requests | [`source-identity.json`](../docs/evidence/CH-001R-r10/source-identity.json) |

The r10 checklist is [`r10-results.json`](../docs/evidence/CH-001R-r10/r10-results.json): `9 PASS / 0 FAIL / 9 NOT_RUN`. The historical T9 hosted ledger remains separate and is not promoted to a new application result.

## Artifacts

Durable evidence is indexed in [`docs/evidence/CH-001R-r10/README.md`](../docs/evidence/CH-001R-r10/README.md) and includes source identity, migration disposition, host-only log, fresh/repeat/schema `NOT_RUN` records, startup/cleanup records, command report, scope comparison, router reproduction, and the checklist. The final durable file digest list is [`artifact-manifest.json`](../docs/evidence/CH-001R-r10/artifact-manifest.json).

The local proof manifest recorded the early no-startup records before hashing them: `compose-startup-state.json` SHA-256 `e151221a6b0811fa9d50b11c6c4ff98df0adca262ae079bb6ab6ffac2ffcf376` and `compose-cleanup.json` SHA-256 `7ae9cfbf5c566ca648fb259f8d7f39a66aeeb8382c8aefcd3a0080bc2c24041b`. No private raw logs, env files, credentials, cookies, or auth storage are included.

## Remaining blockers and external actions

The missing proof requires a Docker-capable host. The router may follow [`router-reproduction.md`](../docs/evidence/CH-001R-r10/router-reproduction.md) using unique synthetic credentials, isolated labeled volumes, exact source identities, bounded state/log capture, and scoped teardown. It must retain `NOT_RUN` if those prerequisites remain unavailable and must not guess at SQL or weaken migration checks.

Luna accessed the package registry for the approved frozen-install retry after the restricted sandbox DNS failure; pnpm reported the lockfile was up to date and reused 460 packages. No Docker image was built, no disposable database/container was created, no host policy was mutated, no provider/account authorization was used, no publication was performed, and no hosted workflow was dispatched or rerun.

## Scope and stop condition

This continuation contains only the bounded coordinator/helper/test changes listed above and their evidence/state records. Providers, AI, publishing, scheduling, analytics, billing, video, deployment/release, global sandbox bypass, worker-isolation redesign, dependency upgrade, and v0.2 work remain excluded. Application acceptance remains false, accepted version remains none, and root remains awaiting review. Stop here for architect review and router publication/reproduction decisions.
