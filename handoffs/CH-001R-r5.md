# Luna/router handoff — CH-001R-r5

## Status

Operational status: `READY_FOR_ROUTER_PUBLISH / NEEDS_WORKFLOW_DISPATCH`.

Application acceptance: **false**. Root: `awaiting_review`. Accepted version: `none`. Target: `0.1.0`.

This is the narrow T5 proof-directory handoff repair. It does not claim v0.1 acceptance, a successful hosted proof, or any v0.2/provider/publishing work.

## Identities

| Field | Actual value |
|---|---|
| Review baseline | `df008ff64d02ada64b8e91578143709ea7b98b89` |
| Packet P5 | `53f7786fdd728140c94bd82e3855cd7e97c96cf5` |
| P5 tree | `9886165add66465f6ee2889027c9abf90ae012a2` |
| T5 implementation / tree | `6849b39f4e3c7a03b5f132418b1d33cc84c98d51` / `85ea5ab159c6f6bd5babf63246cd16c8c50b8655` |
| T5 parent | `53f7786fdd728140c94bd82e3855cd7e97c96cf5` |
| E5 evidence commit | This documentation-only commit; full SHA is returned below and outside this self-referential file. |
| Published state | `git push origin main` succeeded; `git ls-remote origin refs/heads/main` returned T5. |
| Workflow blob / file SHA-256 | `50774746e3ead4922b265af6652a942302743062` / `a9f2a8b3901a86bf3d5fa8f392cc02188785dde8d3e6785bd6243f3f1f6af4ef` |
| Dispatch count from editing box | `0` |

## T5 changes

- Removed the workflow’s premature `CH001_EVIDENCE_ROOT/public` mkdir; the outer workflow now owns only the bootstrap report directory.
- Added the dependency-free shared proof-boundary helper used by the coordinator. It preserves run-ID and repository-contained path validation, rejects any nonempty proof root including an empty `public` child, and creates coordinator-owned `private/commands` and `public/commands` only after the freshness check.
- Removed the coordinator’s pre-try proof-root mkdir. A successful preparation marks the run owned. Refusal skips source/failure report writes in the refused directory, reports the detail on stderr, and returns nonzero. Owned post-preparation failures still write the run-local failure report atomically.
- Added executable R5-T01 through R5-T12 regressions with temporary fixtures, real helper initialization, workflow bootstrap-helper execution, evidence hash preservation, result propagation, and pin/CI regression wiring.

Changed files are exactly:

```text
.github/workflows/ch001-live-proof.yml
scripts/ch001-proof-boundary.d.mts
scripts/ch001-proof-boundary.mjs
scripts/ch001-proof.ts
tests/ci/proof-boundary.test.mjs
```

## Verification

All focused boundary cases passed: `node -e 'import("./tests/ci/proof-boundary.test.mjs")'` returned exit `0` with `12/12` passed. The existing CI module passed `9/9` with exit `0`. The package command `pnpm test:ci` returned exit `0` and Node reported `2/2` file-level tests; the host’s Node 20 test-runner presentation is recorded exactly in [focused-regressions.log](../docs/evidence/CH-001R-r5/focused-regressions.log).

The following post-T5 checks all returned exit `0`: `node --check` for the new helper/tests, `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm test:unit` (`3` files / `20` tests), `pnpm test:security` (`1` file / `4` tests), and `git diff --check`.

Pins were preserved: Node `20.19.2`, pnpm `12.3.4`, lockfile SHA-256 `9c49bf356bdd523623b79cf6096df83de51f06b358bffa88391700a4c5719d70`, package SHA-256 `cb1e1a5c2ec11ecc7d397c9aa7700d76841cede38615656e3e273f34b140dbd0`, and native release manifest SHA-256 `fb6212a43173722f565a4101c311b23b54b7204f4828e0c62e9b9f7af1a8bd6d`.

## Local bounded proof

The fresh T5 local command was:

```text
CH001_RUN_ID=r5-local-t5-boundary CH001_EVIDENCE_ROOT=artifacts/ch001r5/r5-local-t5-boundary/proof pnpm proof:ch001
```

It returned exit `2`, status `BLOCKED_ENVIRONMENT`, and implementation T5. Preparation passed and the old directory collision did not recur. Loopback allocation was denied (`listen EPERM`), Docker/Compose was unavailable (`spawn docker ENOENT`), and pinned Chromium was unavailable at `/home/box/.cache/ms-playwright/chromium-1243/chrome-linux64/chrome`. The local 72-ID ledger is `4 PASS / 0 FAIL / 68 NOT_RUN`, with only source-only IDs `CH001-067`, `CH001-068`, `CH001-069`, and `CH001-072` passing. No live application suite or export acceptance is claimed.

Local run-owned evidence is ignored at `artifacts/ch001r5/r5-local-t5-boundary/proof/public/`. Recorded hashes are: proof result `6ace27aecd04197ae18876c2866ed090a330ce57695c689290f9faa64e36aae8`, gate ledger `d583843dd7693530002bb251b734f325ead419d3168073d8d0680e4dfe163073`, manifest `4cf7242e995436e2ec346ef757dd87235536a4f22e789a76c3d5353a64663d8b`, and environment `94c83f6642f7aea53fc84a2f79c2f93c4f63928b20ceab190fed39a6ccb32b4e`.

## Historical T4 reference

The reviewed T4 run was `34669975078`, attempt `1`, job `103489379533`: bootstrap `PASS`, proof invoked `true`, proof child exit `1`, coordinator `TEST_FAILURE`, and zero coordinator steps because the nonempty `proof/public` collision occurred before preparation. Its artifact was ID `10290367825`, 9,894 bytes, SHA-256 `af14a46a2a85825cb784387b60e6da353b0ff93591607a000b98baf66fb1001d`, expiring `2026-09-26T03:18:35Z`. Hosted gate counts remain unavailable for that historical artifact and are not replaced by local counts.

## Router action

The T5 execution commit is already published to `origin/main`. No external Luna/router send occurred, and no GitHub Actions workflow was dispatched from this editing box. The workflow-capable router should:

1. Verify T5 and this E5 commit/ancestry on `origin/main`, and verify the published workflow blob/SHA-256.
2. Dispatch exactly once with `implementation_sha=6849b39f4e3c7a03b5f132418b1d33cc84c98d51`.
3. Select the new run by dispatch actor/time/event, requested input, and actual checkout; inspect the settled bootstrap/proof reports and downloaded artifact.
4. Preserve a same-collision, environment blocker, or new downstream application failure as evidence and stop for the next architect decision. A bounded success remains `application_acceptance=false`.

Do not rerun T3, old run `34665615514`, or unchanged T4 run `34669975078`; do not add unrelated fixes.
