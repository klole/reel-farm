# Luna/router handoff — CH-001R-r4

## Status

Operational status: `READY_FOR_ROUTER_PUBLISH`.

Application acceptance: **false**. Root: `awaiting_review`. Accepted version: `none`. Target: `0.1.0`.

T4 is committed locally and the E4 evidence/handoff commit is the documentation-only commit containing this file and the r4 evidence/state index updates; its full SHA is returned with this handoff. No execution-affecting change was made after T4.

## Identities

| Field | Actual value |
|---|---|
| Starting source / packet P4 | `a86c3ace9ad8bf1ec941565ce7dbc4e4d734b4e2` |
| T4 implementation SHA / tree | `69b784526260e3e5acf133da8d1a2fb33447d20f` / `55fdf4fc16f1d07498f6fb447cddc64e5261be2c` |
| E4 evidence commit, returned externally | This docs/evidence commit; full SHA returned below/outside this file |
| Remote publication verified | No. No push or Actions dispatch was attempted; local `main` is one commit ahead of `origin/main`, which remains P4 |
| Workflow-definition SHA / blob / SHA-256 | T4 workflow source commit `69b784526260e3e5acf133da8d1a2fb33447d20f`; blob `65f1ae428e925b4747fea03f6c228a8af4acf15b`; file SHA-256 `e41c6ce267c8dacfe759f83c873c0f53ebca90e2c56f19e6d17202267b06b896` |
| Requested and actual checkout SHA | Local checks: requested/actual T4 `69b784526260e3e5acf133da8d1a2fb33447d20f`; hosted: null until router publication and dispatch |
| Run ID / attempt / job ID | Local coordinator `r4-local-t4-preflight` / n/a / n/a; hosted `null` |
| Artifact ID / name / bytes / SHA-256 / expiry | Hosted `null`; local ignored public evidence manifest at `artifacts/ch001r4/r4-local-t4-preflight/proof/public/` |

T4 is directly based on P4. Its execution-affecting diff is exactly seven files: the live-proof workflow, `Dockerfile`, `package.json`, the two dependency-free CI helpers, the pinned native release manifest, and focused CI tests. E4 contains documentation/state/evidence only; it does not invalidate T4 test identity.

## Changes made

- Replaced Corepack startup in Actions and Docker with the shared dependency-free native pnpm bootstrap and checked-in release manifest. The project pin remains `pnpm@12.3.4`; Node remains `20.19.2`; lockfile and application dependency pins are unchanged.
- Bound the official Linux x64 glibc archive to `https://github.com/pnpm/pnpm/releases/download/v12.3.4/pnpm-linux-x64.tar.gz` and SHA-256 `9705e5704b4679fb503c963a18d1ac4f105e39aafafca8a2ed346facdf820cd0`. The helper checks target OS/architecture/libc, pin drift, HTTPS retries/timeouts, digest before extraction/execution, safe tar members, owned/idempotent installation, PATH/GITHUB_PATH, and the resolved version. The arm64 glibc metadata remains checked in with its packet SHA.
- Docker build and final runtime use the same manifest/helper. The final image retains the non-root `node` user and runs a network-disabled native `pnpm --version` probe.
- Added a separate outer bootstrap/CI result record, dependency-free early failure reporting, literal summary formatting, nullable absent-proof semantics, explicit stage/proof outcomes, and fail-closed report/artifact classification. The existing bounded r3 coordinator and strict 72-gate contract are unchanged.
- Added executable positive/negative regressions for the two observed failures and archive/pin/reporting safety cases.

No pins were upgraded, insecure flags or fallback package managers were added, and no application/provider/publishing/video/release/v0.2 work was included.

## Executed commands

All commands below ran on the editing host on `2026-09-12 UTC` against T4 unless noted.

| Command | Host/time | Exit | Discovered/executed/passed/failed/skipped where relevant | Evidence |
|---|---|---:|---|---|
| `node --check scripts/ci/pnpm-bootstrap.mjs`; `node --check scripts/ci/ci-result.mjs`; `pnpm test:ci` | local / 2026-09-12 UTC | 0 | Dependency-free CI regression suite passed; 10 focused cases/subtests, including positive and negative bootstrap/reporting cases | `docs/evidence/CH-001R-r4/ci4-results.json` |
| Fresh official native archive bootstrap, repeat/idempotence, PATH/GITHUB_PATH/version checks, and `pnpm install --frozen-lockfile` | local / 2026-09-12 UTC | 0 | Native pnpm `12.3.4` resolved; lock hash before/after identical (`9c49bf356bdd523623b79cf6096df83de51f06b358bffa88391700a4c5719d70`); no tracked mutation | `docs/evidence/CH-001R-r4/source-review.md` |
| `pnpm lint`; `pnpm typecheck`; `pnpm build` | local / 2026-09-12 UTC | 0 | All completed successfully | `docs/evidence/CH-001R-r4/source-review.md` |
| `pnpm test:unit`; `pnpm test:security` | local / 2026-09-12 UTC | 0 | Unit: 20 passed. Security/scope: 4 passed | `docs/evidence/CH-001R-r4/source-review.md` |
| Docker CLI/Compose availability and shipped-image build/runtime probe | local / 2026-09-12 UTC | unavailable | `docker --version` and `docker compose version` both returned command not found; no image result is claimed | `docs/evidence/CH-001R-r4/ci4-results.json` |
| `CH001_RUN_ID=r4-local-t4-preflight CH001_EVIDENCE_ROOT=artifacts/ch001r4/r4-local-t4-preflight/proof pnpm proof:ch001` | local / 2026-09-12 UTC | 2 | Existing bounded coordinator ran; `BLOCKED_ENVIRONMENT`; 4 PASS / 0 FAIL / 68 NOT_RUN. No live suites or runtime exports executed | `artifacts/ch001r4/r4-local-t4-preflight/proof/public/proof-result.json` (ignored local evidence) |

`pnpm verify:ch001` was not repeated on the known restricted host because its strict profile would repeat unavailable Docker/loopback/browser prerequisites; the verifier and 72-gate contract were not changed. This is not inherited hosted evidence.

## CI outcome

- Primary stage and actual exit: no hosted stage exists yet. Local native bootstrap and frozen install passed with exit `0`; the local bounded proof exited `2` for explicit environment blockers.
- Bootstrap repaired/demonstrated: yes, locally with the official archive and real native executable; hosted demonstration is `NOT_RUN` pending router publication/dispatch.
- Proof invoked: local `true`; hosted `null`.
- Proof exit: local `2`; hosted `null` until dispatch.
- Report validation: dependency-free classifier/regression validation passed locally. No hosted outer report or coordinator report exists.
- Summary outcome: hostile literal summary and failure-preservation regressions passed locally; hosted summary is `NOT_RUN`.
- Artifact transport and independent inspection: no hosted upload/receipt. Local coordinator wrote 25 public files and a public manifest; private logs/environment were not included in the public path.
- CI4 checks: `17 PASS / 0 FAIL / 7 NOT_RUN`.
- Fresh parent CH001 ledger: local `r4-local-t4-preflight`, `4 PASS / 0 FAIL / 68 NOT_RUN`, status `BLOCKED_ENVIRONMENT`.
- Remaining original gates/findings: the historical strict CH-001 ledger remains `0 PASS / 0 FAIL / 72 NOT_RUN`; no application acceptance decision changed. The local four source-only passes do not close runtime, visual, lifecycle, export, manual, or architect-review requirements.

The local environment blockers were loopback `listen EPERM`, missing Docker/Compose (`spawn docker ENOENT`), and absent/unlaunchable pinned Chromium at `/home/box/.cache/ms-playwright/chromium-1243/chrome-linux64/chrome`. No application failure was exposed because the live application assertions were not reached.

## Artifact inventory

The local run-owned public directory is `artifacts/ch001r4/r4-local-t4-preflight/proof/public/` (gitignored). It contains the coordinator's 25-file public inventory: `artifact-manifest.json`, `command-report.json`, `dispatch.json`, `environment.json`, `finding-dispositions.json`, `gate-results.json`, `proof-result.json`, `sanitization.json`, `security-results.json`, `smoke-results.json`, `suite-e2e-unavailable.log`, `suite-e2e.json`, `suite-integration-unavailable.log`, `suite-integration.json`, `suite-render-unavailable.log`, `suite-render.json`, `suite-smoke.json`, `unit-results.json`, `verifier-result.json`, and the remaining coordinator public reports recorded by that manifest.

Recorded local hashes:

- `proof-result.json`: `cb50136e5d752deb731caeea61c75542cde43f0c5f58e4fd67b1f32862aaa8f7`
- `gate-results.json`: `eb20e14df937109fdb08ccadd20d321ce98a0296e593eb38b395b9a195e070fe`
- `artifact-manifest.json`: `30f695e1db0a7920c7cc06ca8c0ab30172810428d62cec9b53a1687f00bbefb1`
- `environment.json`: `4f92af92794b9deb04aa42103961c46bbe6be7d03d72f8497f4b9d88f0b5c991`
- `command-report.json`: `3df7e75f1d32c6f3f049b46043dc313bd6ae417d43bfde4208c4d5e9c76f905c`
- `sanitization.json`: `46f2e12272d39ed43b9c89c2b37b27fd4b79dba2df6c997632d4294452ec7840`

The local manifest resolves its referenced public paths. No canonical seven-slide ZIP, alternate ZIP, preview byte package, screenshots, shipped image, or lifecycle report exists. No hosted artifact ID, archive bytes, digest, retention date, or failure-step receipt exists yet; the historical failed run `34665615514` was not rerun and is not used as T4 evidence.

## Scope, credentials and external actions

- Network activity was limited to downloading the official pnpm `12.3.4` Linux archive into temporary run-owned storage for checksum/layout inspection and the real local bootstrap. Its digest matched the checked-in manifest. No credentials were copied, requested, persisted, or embedded.
- Workflow-capable publication actor/path: not used by Luna. Manual dispatch count: `0`.
- No providers, accounts, publishing, scheduling, billing, deployment, releases/tags, video/MP4, analytics, or v0.2 work occurred. No external test resources were created.

## Next action and stop

The router must publish the preserved T4 and E4 commits through the already-authorized workflow-capable route, verify the exact commits and workflow identity on `origin/main`, then perform exactly one fresh manual dispatch. After replacing `E4` with the full SHA returned with this handoff:

```sh
REPO=klole/reel-farm
T4=69b784526260e3e5acf133da8d1a2fb33447d20f
E4=REPLACE_WITH_THIS_HANDOFFS_FULL_E4_SHA

git fetch origin main
git cat-file -e "$T4^{commit}"
git cat-file -e "$E4^{commit}"
git merge-base --is-ancestor "$T4" "$E4"
git merge-base --is-ancestor "$E4" origin/main
gh api "repos/$REPO/commits/$T4" --jq .sha
gh api "repos/$REPO/commits/$E4" --jq .sha
gh api "repos/$REPO/contents/.github/workflows/ch001-live-proof.yml?ref=main" --jq .sha
gh workflow run ch001-live-proof.yml --repo "$REPO" --ref main -f implementation_sha="$T4"
gh run list --repo "$REPO" --workflow ch001-live-proof.yml --event workflow_dispatch --limit 10 \
  --json databaseId,headSha,createdAt,status,conclusion,url
```

Select the one new run by dispatch time, actor/event, workflow identity, requested implementation, and actual checkout. Record its attempt/job/artifact IDs, inspect the downloaded archive and payload hashes, and update the r4 receipt. Do not rerun `34665615514`, dispatch T3, or mark a green bounded proof as v0.1 acceptance. If the repaired bootstrap succeeds and the application proof exposes a new application failure, preserve the exact hosted evidence and stop for architect review; do not implement feature work.

Historical r3 records remain intact. Root state remains `awaiting_review`, `application_acceptance=false`, and accepted version `none`.
