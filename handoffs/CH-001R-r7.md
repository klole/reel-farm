# Luna handoff — CH-001R-r7

## State

- Operational status: `READY_FOR_ROUTER_PUBLISH`.
- Target application version: `0.1.0`.
- Root state: `awaiting_review`.
- Application acceptance: `false`.
- Accepted application version: `none`.
- This is workflow validation and dispatch repair only; it is not v0.2 or full acceptance.

## Identities

| Field | Actual value |
|---|---|
| Reviewed E6 / T6 | `a4d6e7a0149d6852bf348d6277c4200720320f9c` / `e0ea57665d00a643a8c392dfb9f6a84a723729af` |
| Base/P7 and tree | `9fb748ef6b22a21ed24a8a5fa66e4ec1463a049a` / `ea70e7e6b11cc2c44c7e4213fe06c369d2fea9dc` |
| T7 implementation and tree | `6b02857401a9b1e81e1bd36d5a4418f90903adcb` / `fdbe7da15964659d4e2514580f6606f5731790ce` |
| T7 parent chain | `d3f6b2680d2257c41df82faa5ae686559cf80a46` → T7 |
| Workflow blob and SHA-256 at T7 | `2070e80be5355537631a7b2cf833eeb0a6cf28cb` / `98808688f0261296f6c0ddd0b3b34b04e67bc2d74735660512ab6c1852849c51` |
| E7 evidence commit | Returned externally after commit; not embedded self-referentially |
| Publication on origin/main | `NOT_RUN`; local `origin/main` remained P7 |
| W7 definition commit | `null` |
| Validator | actionlint `1.7.7`; executable SHA-256 `9f7dedb4e23f89f2922073d1a6720405b7b520d4f5832ebb96f0d55a2958886c` |
| Validator archive | official Linux/amd64 v1.7.7; SHA-256 `023070a287cd8cccd71515fedc843f1985bf96c436b7effaecce67290e7e0757` |

Machine-readable identity and validation records are in [r7 evidence](../docs/evidence/CH-001R-r7/README.md).

## Root-cause disposition

- R7-F01: removed the unsupported `CH001_SANDBOX_STATE_DIR` expression from `jobs.live-proof.env`. The existing `identity` step invokes `scripts/ci/initialize-sandbox-state.sh` after source identity is established. The helper validates inputs and appends the exact r6 path, `${RUNNER_TEMP}/ch001r6/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/sandbox`, to `GITHUB_ENV` for subsequent steps.
- R7-F02: added `pnpm lint:workflow`, backed by a version-checked actionlint `1.7.7` wrapper with tracked-workflow enumeration, input hashes, command/tool metadata, explicit `-shellcheck=` and `-pyflakes=` flags, and fail-closed tooling/input handling.
- No additional workflow definition error was exposed by actionlint after the bounded repair.
- The r5 coordinator-owned proof directory, r6 exact executable/sandbox policy, `chromiumSandbox=true`, explicit boolean opt-in, public/manual hosted guard, non-root worker defaults, and `always()` cleanup remain intact.

## Actual command results

The final checks ran against T7 after the last implementation commit. See [commands.log](../docs/evidence/CH-001R-r7/commands.log) and [workflow-validation.log](../docs/evidence/CH-001R-r7/workflow-validation.log).

| Command | Exit | Evidence |
|---|---:|---|
| `node --check scripts/ci/lint-workflow.mjs` + `bash -n scripts/ci/initialize-sandbox-state.sh` | 0 | commands.log |
| `ACTIONLINT_BIN=…/actionlint pnpm lint:workflow` | 0 | workflow-validation.json/log |
| `ACTIONLINT_BIN=…/actionlint pnpm test:ci` | 0 | 4/4 file-level CI files |
| `pnpm lint` | 0 | commands.log |
| `pnpm typecheck` | 0 | commands.log |
| `pnpm build` | 0 | commands.log |
| `pnpm test:unit` | 0 | 3/3 files, 20/20 tests |
| `pnpm test:security` | 0 | 1/1 file, 4/4 tests |
| `git diff --check` | 0 | commands.log |

The direct individual CI suite counts were: existing r4 helper `10/10`, r5 boundary `12/12`, r6 sandbox `18/18`, and r7 validation `10/10`. The aggregate `pnpm test:ci` count is deliberately reported separately as `4/4` file-level tests.

`pnpm proof:ch001` was not repeated because r6 already established unavailable editing-host prerequisites (Docker/Compose, loopback, and pinned Chromium); no local runtime qualification is claimed. `pnpm verify:ch001` was not changed or used to promote this repair.

## R7 checklist

| ID | Status | Actual evidence / boundary |
|---|---|---|
| R7-T01 | PASS | Real actionlint rejected complete T6 bytes at line 39; frozen SHA is recorded. |
| R7-T02 | PASS | Same validator accepted complete T7 workflow at the T7 hash. |
| R7-T03 | PASS | Executable controls cover missing, wrong-version, nonzero, zero-input, and malformed cases. |
| R7-T04 | PASS | Real semantic validator rejected `steps.missing` and accepted the step-level runtime fixture. |
| R7-T05 | PASS | Separate child consumers observed the same absolute run/attempt path. |
| R7-T06 | PASS | Spaces/shell-looking data stayed literal; no env-file evaluation. |
| R7-T07 | PASS | Missing/relative/multiline/invalid inputs failed before modifying existing entries. |
| R7-T08 | PASS | No proof/policy tree was created; prior evidence stayed intact; r5 suite passed. |
| R7-T09 | PASS | Opt-in, guard, exact browser/sandbox and cleanup wiring retained. |
| R7-T10 | PASS | Existing r4/r5/r6 helper suites passed with counts above. |
| R7-T11 | PASS | Lockfile/native manifest/dependency and runtime pins unchanged; package diff is one script. |
| R7-T12 | PASS | T7/tree/workflow hashes and clean tracked source are recorded; E7 is evidence/state-only. |
| R7-T13 | NOT_RUN | Router must fetch publication tree and verify W7/T7 workflow bytes. |
| R7-T14 | NOT_RUN | No dispatch was submitted from this workspace; router owns at most one fresh request. |
| R7-T15 | NOT_RUN | No new hosted run, job, artifact, sandbox, worker, or proof result exists. |
| R7-T16 | PASS | Historical records are preserved and no live identifiers/secrets were invented. Hosted portion remains router-owned. |

## Historical r6 dispatch boundary

The historical T6 manual request remains `REJECTED_BEFORE_RUN`, HTTP 422 as supplied in the reviewed baseline, because `runner.temp` was unsupported in job-level env. Its live proof run/job/artifact IDs, proof invocation, proof exit, and hosted gate counts remain null/false. The E6 push-validation record `34675672523` remains a separate `push` failure with zero jobs and is not a live-proof run. Full sanitized record: [dispatch-rejection-history.json](../docs/evidence/CH-001R-r7/dispatch-rejection-history.json).

## New T7 router outcome

- Publication verified: `NOT_RUN`.
- Pre-publication semantic validation on publication tree: `NOT_RUN` (must be rerun by router; local T7 validation is recorded separately).
- `sent`: `no` to GitHub; handoff is prepared for the existing authorized workflow-capable router.
- `workflow_dispatch_submitted`: `false`.
- Request outcome: `null`.
- New run/attempt/job/artifact: `null`.
- Sandbox requested/measured qualified: `null` / `null`.
- Worker qualified: `NOT_RUN`.
- Proof invoked/actual exit: `false` / `null`.
- Policy cleanup: `NOT_RUN`.
- Application acceptance: `false`.

## Scope and stop

Changed paths are:

- `.github/workflows/ch001-live-proof.yml`
- `package.json` (one scripts-only `lint:workflow` entry)
- `scripts/ci/actionlint-provenance.json`
- `scripts/ci/initialize-sandbox-state.sh`
- `scripts/ci/lint-workflow.mjs`
- `tests/ci/workflow-validation.test.mjs`
- `tests/fixtures/workflow-validation/invalid-steps.yml`
- `tests/fixtures/workflow-validation/invalid-yaml.yml`
- `tests/fixtures/workflow-validation/valid-runtime.yml`
- E7 evidence/state/handoff records under `docs/evidence/CH-001R-r7/`, `state/`, and this handoff.

The lockfile, native pnpm manifest, dependency versions, Node/Playwright pins, application code, Dockerfile, Compose controls, original 72-gate contract, strict verifier, and r6 sandbox policy semantics were not weakened or redesigned. No provider/publishing work, account authorization, paid action, external browser, privileged host mutation, global policy relaxation, release/deployment, video, scheduling, or v0.2 work occurred. Router publication and the one optional fresh T7 dispatch are the only remaining authorized external actions; stop for router/architect review.
