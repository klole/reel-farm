# Luna handoff — CH-001R-r2 / v0.1.0

Status: `BLOCKED` — implementation and real harnesses are delivered, but the host could not establish PostgreSQL, the pinned Chromium runtime, or Docker Compose. This is not an acceptance claim and does not authorize v0.2.

## Control record

| Field | Actual value |
|---|---|
| Chapter / parent | `CH-001R-r2` / `CH-001-r1` |
| Repository / branch | `klole/reel-farm` / `main` |
| Follow-up starting commit | `c05ac9c8753fc4237188f9f8b5c5b7250a5fce78` |
| Reviewed implementation / evidence | `bdd6540b90f2d59b7ef11215cd0ceb631fc6336a` / `ad0dd5fdd6764e4f3a40a6028720bff272130d42` |
| Original historical result | `0 PASS / 0 FAIL / 72 NOT_RUN`, preserved unchanged |
| Implementation commit T | `b516dab843be1b8870d3516185b52905982aec1f` |
| Evidence/handoff commit E | This file's containing commit; exact full SHA is returned after commit creation per the two-commit protocol |
| Target | Open Slideshow Studio `v0.1.0` |
| North Star | NS-0.2; SHA-256 `3175b197c221588fa92273c9f8403ecbcc2357fe486f63a97365205d951ad80d` |
| Run | `r2-20260911-blocked` |
| Tested tree | T was committed before the aggregate; no application/test changes followed T. Post-T working-tree changes are state/evidence documentation only. |
| Root state | `awaiting_review`; accepted application version: `none` |

The original CH-001-r1 evidence remains historical. The fresh run is under [docs/evidence/CH-001R-r2/](../docs/evidence/CH-001R-r2/README.md), with machine-readable results in [gate-results.json](../docs/evidence/CH-001R-r2/gate-results.json).

## Implementation delivered

T replaces the placeholder live-command guards with real PostgreSQL, Playwright, renderer, and disposable Compose harnesses. It adds a fail-closed verifier that loads the original contract, requires exactly 72 unique IDs, binds evidence to T, checks command/suite discovery and execution, validates evidence hashes/paths and artifacts, requires named manual inspection for visual/manual gates, and exits 1 for incomplete coverage.

The scoped repairs include final-image browser dependencies and existing non-root `node` execution; explicit `chromiumSandbox: true` with a narrow browser environment; loopback-only configurable Compose binding; storage read/write probes; truthful database/storage/worker/renderer readiness; separate asset refresh in the editor; stable acknowledged-head and mutation identities across queued/uncertain saves; stale-load invalidation; pre-parse bounded multipart receipt; explicit supported slide-glyph diagnostics; awaited font/image decode; lease/deadline checks; attempt-ceiling enforcement across stale recovery; attempt-scoped render cleanup; and fenced artifact finalization.

No provider, publishing, billing, scheduling, analytics, direct official TikTok API, MP4, marketplace, public deployment, release, or v0.2 work was added.

## Commands and actual results

The exact command records and logs are in [command-report.json](../docs/evidence/CH-001R-r2/command-report.json) and [01-pnpm-lint.log](../docs/evidence/CH-001R-r2/01-pnpm-lint.log) through `09-pnpm-test-smoke.log`.

| Procedure | Result |
|---|---|
| `pnpm install --frozen-lockfile` | Final approved retry exit 0; frozen lockfile unchanged and 460 packages reused. A preceding sandboxed attempt could not resolve the npm registry and was stopped after retries; this limitation is recorded in [PREFLIGHT.md](../docs/chapters/CH-001/PREFLIGHT.md). |
| `pnpm lint` | exit 0 |
| `pnpm typecheck` | exit 0 |
| `pnpm build` | exit 0; production Next build and all package/worker builds completed |
| `pnpm test:unit` | exit 0; 3 files / 19 tests passed |
| `pnpm test:security` | exit 0; 1 file / 4 tests passed |
| `pnpm test:integration` | exit 2; `DATABASE_URL` absent; 0 discovered / 0 executed |
| `pnpm test:e2e` | exit 2; pinned Playwright Chromium absent at the path recorded by the runner; 0 discovered / 0 executed |
| `pnpm test:render` | exit 2; pinned Playwright Chromium absent; 0 discovered / 0 executed |
| `pnpm test:smoke` | exit 2; Docker CLI unavailable; 0 discovered / 0 executed |
| `pnpm verify:ch001` | exit 1; all nine root commands ran, fail-closed validation reported 13 issues |

The final verifier reports five root commands at exit 0 and four live commands at exit 2. Exit 2 is retained in the child reports; it is not promoted to a pass.

## Fresh 72-gate ledger

The fresh ledger has exactly 72 IDs: `4 PASS / 0 FAIL / 68 NOT_RUN`.

| Status | Gate IDs |
|---|---|
| PASS | `CH001-067`, `CH001-068`, `CH001-069`, `CH001-072` — E1 source/provenance/scope/state inspection in [source-review.md](../docs/evidence/CH-001R-r2/source-review.md) |
| NOT_RUN | `CH001-001` through `CH001-066`, `CH001-070`, `CH001-071` — required live DB/browser/worker/Compose/lifecycle/visual/manual/artifact or handoff proof was not obtained |
| FAIL | none |

The `PASS` entries are only the original contract's source-only gates. No runtime, visual, keyboard, export, database, browser, lifecycle, or Compose gate was upgraded from source inspection. The exact individual rows, reasons, evidence references, and hashes are in [gate-results.json](../docs/evidence/CH-001R-r2/gate-results.json). The historical ledger remains `0 PASS / 0 FAIL / 72 NOT_RUN` in [docs/evidence/CH-001/gate-results.json](../docs/evidence/CH-001/gate-results.json).

## R01–R11 dispositions

| Finding | Disposition in T and fresh run |
|---|---|
| R01 | Placeholder paths replaced by real integration, E2E, render, and Compose harnesses; independent runs prove they fail with explicit exit 2 when prerequisites are absent. Capable-environment execution remains open. |
| R02 | Aggregate now rejects missing/duplicate/unknown IDs, empty suites, stale commits, missing evidence, bad hashes, failed commands, NOT_RUN coverage, missing contract, and missing manual review; 12 verifier tests passed. Full all-pass evaluation remains open. |
| R03 | Final image installs runtime browser libraries and uses the base image's `node` user with volume ownership; Docker build/run was not possible. |
| R04 | Worker and Playwright config explicitly enable Chromium sandboxing and pass no application secrets; effective shipped-container proof was not possible. |
| R05 | Upload refresh no longer reloads the draft; the real UI journey includes an upload during unsaved text and history state. Browser execution was unavailable. |
| R06 | Saves resolve the acknowledged head at dispatch, retain immutable mutation identity, retry uncertain older saves before newer snapshots, and invalidate stale reload work. Two-tab/delayed/lost-response execution was unavailable. |
| R07 | DB/storage probes, pre-heartbeat browser capability probe, truthful degraded/offline states, and independent editor polling were implemented. Stop/restart/storage-fault execution was unavailable. |
| R08 | Stale recovery and claim paths enforce the three-attempt ceiling; leases/deadlines fence finalization and attempt-owned files are cleaned. Crash/reclaim lifecycle execution was unavailable. |
| R09 | Multipart bytes are bounded before `formData()` with a missing/untrusted `Content-Length` path; per-file and batch decoder limits remain. HTTP adversarial execution was unavailable. |
| R10 | Fonts and images have bounded readiness/decode barriers; unsupported slide glyphs and overflow are actionable validation failures. Real browser resource execution was unavailable. |
| R11 | Fresh sanitized command, suite, environment, source-review, verifier, and artifact-manifest files are committed with hashes; requested runtime artifacts and human inspection remain absent. |

These are dispositions, not claims that an unavailable runtime scenario passed. The remaining open limitation is environmental evidence, not a fabricated application failure.

## User journey and output evidence

The primary seven-slide UI journey did not complete on this host. The real test is [tests/e2e/primary-journey.spec.ts](../tests/e2e/primary-journey.spec.ts) and uses the exact `A little room to focus` title, seven slide copy/layouts, local generated fixture images, duplicate/move/remove, keyboard add/remove, save/reopen, preview, ZIP extraction, manifest checks, post-text equality, and preview-response/ZIP byte/hash comparisons. The render suite covers all three layouts at both 1080×1920 and 1080×1350 presets and tests delayed valid versus missing image decode.

No application-exported ZIP, screenshot, final-preview JPEG, alternate-format output, visual diff, post hash, image hash set, or response-body comparison was produced. The evidence directory intentionally contains no placeholder artifact. Its [artifact-manifest.json](../docs/evidence/CH-001R-r2/artifact-manifest.json) lists only files that exist.

## Environment and remaining blockers

[environment.json](../docs/evidence/CH-001R-r2/environment.json) records Debian/Linux x64, Node `v20.19.2`, pnpm `12.3.4`, no configured database URL, and absent runtime prerequisites. The host also lacked Docker, Docker Compose, PostgreSQL tooling/service, and managed Playwright Chromium. A system Google Chrome `151.0.7922.169` exists, but no explicit compatible browser profile or database/Compose stack was established, so it was not substituted for the pinned shipped-browser proof.

To complete the checkpoint, the next authorized runner must use a disposable capable environment, run `pnpm install --frozen-lockfile`, run `pnpm test:smoke` to build and launch the final Compose image, let its real DB-backed E2E execute, run integration and render suites, inspect the generated screenshots/output manually, add named inspection evidence, and rerun `pnpm verify:ch001`. Any remaining assertion or artifact mismatch must stay NOT_RUN/FAIL as appropriate. No source change is implied by this runbook.

## Security, provenance, and external actions

Compose publishes only the configurable loopback web port; DB/worker have no host ports. Worker/web run as `node`, worker drops all capabilities and uses no-new-privileges. Accepted media remains behind owner-authorized routes and generated safe storage keys. The browser receives a narrow environment without `DATABASE_URL`, auth secrets, cookies, or a no-sandbox fallback. Source review and focused security tests are supplemental; they are not runtime containment proof.

The only network activity was dependency/package metadata retrieval and the requested push to this already-authorized repository. Paid provider API requests/spend: `0 / none`. ScrapeCreators, fal.ai, LLM, Pinterest, TikTok, publishing, account authorization, billing, analytics, public deployment, release/tagging, and public posts: none. No CI run was dispatched. No normal application data or remote volume was deleted; no disposable Compose environment was created because Docker was unavailable.

## Durable state and stop

Root project/requirement/evidence state points to CH-001R-r2, T, the fresh evidence path, and this handoff while remaining `awaiting_review` with accepted version unset. The next action is architect review of T and E. Stop here; no next feature chapter began.
